# Test Environment Configuration
NODE_ENV=test

# Database Configuration
DB_HOST=localhost
DB_PORT=5433
DB_NAME=japanese_real_estate_test
DB_USER=test_user
DB_PASSWORD=test_password
DATABASE_URL=postgresql://test_user:test_password@localhost:5433/japanese_real_estate_test

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6380
REDIS_PASSWORD=
REDIS_URL=redis://localhost:6380

# API Configuration
PORT=3002
API_BASE_URL=http://localhost:3002
CORS_ORIGIN=http://localhost:3000

# Frontend Configuration
REACT_APP_API_BASE_URL=http://localhost:3002
REACT_APP_ENVIRONMENT=test

# Logging
LOG_LEVEL=error

# Scraping Configuration (faster for tests)
SCRAPING_DELAY_MIN=100
SCRAPING_DELAY_MAX=200
MAX_CONCURRENT_SCRAPERS=1

# Translation (mock for tests)
GOOGLE_TRANSLATE_API_KEY=test_key
TRANSLATION_SERVICE=mock

# Scheduler (disabled for tests)
SCHEDULER_DAILY_ENABLED=false